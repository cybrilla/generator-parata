{
  "name": "<%= _s.slugify(config.name) %>",
  "version": "0.1.0",
  "dependencies": {

  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-copy": "^0.8.0",<% if(config.isUsingSass()) { %>
    "grunt-contrib-sass": "^0.9.2",<% } else if(config.isUsingLess()) { %>
    "grunt-contrib-less": "^1.0.1",<% } %>
    "grunt-contrib-connect": "^0.10.1",
    "grunt-contrib-watch": "^0.6.1",
    "parata": "^0.0.3"
  }
}
